<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="100%" height="100%">
  <defs>
    <!-- Background Gradient -->
    <linearGradient id="bgGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#0F172A"/>
      <stop offset="100%" stop-color="#1E293B"/>
    </linearGradient>

    <!-- Outer Project Frame Gradient -->
    <linearGradient id="projectGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#38BDF8"/>
      <stop offset="100%" stop-color="#0284C7"/>
    </linearGradient>

    <!-- Flow Line & Active Task Gradient -->
    <linearGradient id="taskGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#34D399"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>
  </defs>

  <!-- Dark Rounded Square Base -->
  <rect x="0" y="0" width="32" height="32" rx="7" fill="url(#bgGrad)"/>

  <!-- Outer Project Boundary (Parent Frame) -->
  <rect x="5.5" y="5.5" width="21" height="21" rx="5" fill="none" stroke="url(#projectGrad)" stroke-width="1.8"/>

  <!-- Flow Path inside the project -->
  <path d="M 11 11 C 15 11, 17 21, 21 21" fill="none" stroke="#38BDF8" stroke-width="1.6" stroke-linecap="round"/>

  <!-- Task Nodes -->
  <!-- Top-Left Task (Source Node) -->
  <circle cx="11" cy="11" r="2" fill="#38BDF8"/>

  <!-- Bottom-Right Task (Completed Flow Node) -->
  <circle cx="21" cy="21" r="2.8" fill="url(#taskGrad)"/>

  <!-- Checkmark inside Completed Node -->
  <path d="M 19.8 21 L 20.7 22 L 22.2 20.2" fill="none" stroke="#FFFFFF" stroke-width="0.9" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
